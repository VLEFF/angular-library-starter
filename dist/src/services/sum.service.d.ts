export declare class SumService {
    /**
     * Stores the last sum.
     */
    sum: number;
    /**
     * Calculates the sum.
     * @param addends Numbers to be added
     */
    calculate(...addends: number[]): void;
}
